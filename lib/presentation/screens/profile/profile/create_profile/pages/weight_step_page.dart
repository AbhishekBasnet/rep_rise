import 'package:flutter/cupertino.dart';

class WeightStepPage extends StatelessWidget {
  const WeightStepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('step goal');
  }
}
